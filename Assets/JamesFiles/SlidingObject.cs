using UnityEngine;
using System.Collections;

public class SlidingObject : MonoBehaviour {

	//If an object has a sliding object component, it
	//	keeps moving with its x velocity when it hits the ground
}
